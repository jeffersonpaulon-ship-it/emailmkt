RewriteEngine On

# Serve arquivos e diretórios existentes diretamente (assets, etc).
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo o restante passa pelo front controller.
RewriteRule ^ index.php [L]
